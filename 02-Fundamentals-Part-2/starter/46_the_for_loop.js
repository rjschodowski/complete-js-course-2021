'use strict';

//Just as the 'if/else' statement, LOOPS are also CONTROL STRUCTURES.

//LOOPS allow us to automate tasks we have to perform repetitively.


//For loop keeps running while condition is TRUE. Also, remember that rep ++ is = rep = rep + 1// {}
for(let rep = 1; rep <= 10; rep++) {
    console.log(`Lifting weights repetition ${rep}.`);
}