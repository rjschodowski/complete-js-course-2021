'use strict';

//the DOM always starts with the document object.

/*And document is a special object that we have access to in JavaScript. And this object serves as an entry point
into the DOM. Document is the entry point to the DOM, because we need it to start selecting elements.

The DOM and DOM methods are actually part of something called the web APIs. So the web API APIs are like libraries
that browsers implement and that we can access from our JavaScript code.

Besides the DOM, there are actually a ton more web APIs, such as timers, the fetch API, and many more.*/